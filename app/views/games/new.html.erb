<div class="container">
    <h1>Create a new game</h1>

    <%= form_for @new_game do |f| %>
        <%= f.label :title %>
        <%= f.text_field :title, required: true %>

        <%= f.label :description%>
        <%= f.text_field :description %>

        <%= label_tag :teams, "Add team names separated by a comma" %>
        <%= text_area_tag :teams %>

        <%= f.submit "Create", class:"btn submit" %>
    <% end %>

</div>
